<view class="user-avatar avatar-{{size}} avatar-{{shape}}" bindtap="onTap">
  <image
    class="avatar-image"
    src="{{src || 'https://lovebeyonddays.com/common/default-avatar.png'}}"
    mode="aspectFill"
    lazy-load="{{true}}"
    binderror="onImageError">
  </image>
</view>
