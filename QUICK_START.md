# 快速启动指南

让小程序立即运行起来的最简步骤。

## 5分钟快速启动

### 1. 打开项目（1分钟）

1. 打开**微信开发者工具**
2. 选择"导入项目"
3. 选择项目目录：`D:\Project\M`
4. 填写 AppID（或选择"测试号"）
5. 点击"导入"

### 2. 构建 npm（1分钟）

1. 等待项目加载完成
2. 点击菜单栏"工具" → "构建 npm"
3. 等待构建完成（约10秒）
4. 看到"构建完成"提示

### 3. 编译运行（1分钟）

1. 点击"编译"按钮
2. 等待编译完成
3. 查看预览器中的小程序

### 4. 验证功能（2分钟）

- ✅ 首页显示3条测试内容
- ✅ 点击"动态"Tab 切换到动态页
- ✅ 点击"我的"Tab 切换到我的页面
- ✅ 看到"未登录"提示和"立即登录"按钮
- ✅ 首页筛选功能正常工作

**恭喜！小程序已成功运行！** 🎉

---

## 当前状态说明

### ✅ 可以正常使用的功能

1. **界面展示**
   - 首页、动态页、我的页面全部正常显示
   - TabBar 正常切换
   - 所有样式正确渲染

2. **基础交互**
   - 类型筛选（首页）
   - 下拉刷新
   - 上拉加载
   - 按钮点击

3. **开发功能**
   - Less 样式自动编译
   - npm 包管理
   - 代码热更新

### ⏸️ 需要配置才能使用的功能

1. **云函数相关**
   - 获取真实数据（目前使用模拟数据）
   - 用户登录
   - 数据上传

2. **高级功能**
   - 图片上传
   - 位置选择
   - 消息通知

---

## 模拟数据说明

为了让小程序可以立即运行，以下页面使用了模拟数据：

### 首页（官方内容）
```javascript
显示 3 条测试公告/新闻
- 欢迎使用小程序
- 功能更新通知
- 开发指南
```

### 动态页
```javascript
显示 1 条测试动态
- 测试用户发布的欢迎动态
```

### 我的页面
```javascript
显示默认用户信息
- 昵称：未登录
- 签名：点击登录按钮进行登录
- 统计数据：全部为 0
```

---

## 下一步操作

### 选项 A：继续开发界面（推荐）

适合：先完成界面开发，后续再对接数据

```
✅ 小程序已可正常预览
→ 继续开发和完善界面
→ 使用模拟数据测试交互
→ 完成后再配置云开发
```

### 选项 B：配置云开发

适合：需要立即使用真实数据和登录功能

参考文档：`README.md` 第5-8节

#### 步骤：

1. **开通云开发**
   - 工具 → 云开发 → 开通
   - 创建环境，获取环境 ID

2. **配置环境 ID**
   ```javascript
   // miniprogram/app.js 第17行
   env: 'cloud1-xxx', // 替换为你的环境 ID
   ```

3. **创建数据库**
   - 云开发控制台 → 数据库
   - 创建集合：users, official_content, user_dynamics
   - 设置权限（参考 README.md）

4. **上传云函数**
   - 右键 cloudfunctions/login → 上传并部署
   - 右键 cloudfunctions/getOfficialContent → 上传并部署
   - 右键 cloudfunctions/getUserDynamics → 上传并部署

5. **添加测试数据**
   - 在 official_content 集合中手动添加数据
   - 参考 README.md 第8节

---

## 开发工作流

### 样式开发

1. 编辑 `.less` 文件
2. 保存后自动编译为 `.wxss`
3. 预览器自动刷新

详细说明：`LESS_GUIDE.md`

### 页面开发

1. 修改 `.wxml`、`.js` 文件
2. 保存后自动编译
3. 预览器自动刷新

### 功能开发

1. 使用模拟数据测试交互逻辑
2. 完成后替换为云函数调用
3. 测试真实数据

---

## 常见问题

### Q1：编译后样式没有生效？
**A:**
1. 检查是否已构建 npm（工具 → 构建 npm）
2. 检查 `.less` 文件是否有语法错误
3. 尝试重启微信开发者工具

### Q2：页面显示空白？
**A:**
1. 查看控制台是否有错误
2. 检查 `app.js` 是否正确加载
3. 参考 `TROUBLESHOOTING.md`

### Q3：TabBar 图标不显示？
**A:**
这是正常的，图标文件需要你自己添加：
- 在 `static/tabbar/` 目录下放置图标
- 或者修改 `app.json` 中的图标路径
- 详见 README.md 第7节

### Q4：想使用真实数据？
**A:**
按照"下一步操作 → 选项 B"配置云开发

### Q5：控制台显示云函数错误？
**A:**
这是正常的，因为云开发未配置。小程序会自动使用模拟数据，不影响界面展示。

---

## 测试清单

使用以下清单验证小程序是否正常：

```
基础功能：
□ 小程序正常启动
□ 首页显示内容列表
□ 筛选功能正常（全部/公告/新闻/活动）
□ 动态页显示动态列表
□ 我的页面显示用户信息
□ TabBar 可以切换

交互功能：
□ 下拉刷新不报错
□ 上拉加载不报错
□ 点击卡片有反馈
□ 点击按钮有响应

样式功能：
□ 页面样式正常显示
□ 颜色、字体、间距正确
□ Less 变量生效
□ 动画过渡正常
```

---

## 提示和技巧

### 💡 快速定位问题
```
1. 查看控制台 Console 标签
2. 查看控制台 Sources 标签（查看编译后的代码）
3. 使用调试器断点
```

### 💡 修改模拟数据
```javascript
// pages/index/index.js 第94行
loadMockData(isPullRefresh = false) {
  const mockData = [
    // 在这里添加或修改测试数据
  ];
}
```

### 💡 查看编译后的样式
```
控制台 → Sources → pages/xxx/xxx.wxss
可以看到 Less 编译后的实际样式
```

### 💡 性能优化
```
1. 开启"上传代码时自动压缩"
2. 开启"上传代码时自动压缩图片"
3. 在 project.config.json 中已配置
```

---

## 相关文档

- `README.md` - 完整的项目文档
- `LESS_GUIDE.md` - Less 使用指南
- `TROUBLESHOOTING.md` - 问题诊断和修复
- `CHANGELOG.md` - 更新日志

---

## 技术支持

遇到问题？

1. 📖 先查看相关文档
2. 🔍 在控制台查看错误信息
3. 🐛 查看 `TROUBLESHOOTING.md`
4. 💡 搜索小程序官方文档

---

**祝开发顺利！** 🚀

最后更新：2026-01-04
